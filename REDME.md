1️⃣ What is the difference between var, let, and const?
**ans:the difference between var ,let and const is   let and const those are updated version from es6 they have many difference such as var   maintain function scope but let and const maintain block scop . in a same file we can't declare same name in function variable array object when we use let and const but when we use var we can use same name.let reassign able but const is constant value.   
2️⃣ What is the spread operator (...)?
**ans: The spread operator is a es6 function it is a way that to copy a object and array elements. 
3️⃣ What is the difference between map(), filter(), and forEach()?
**ans: The difference between map() ,filter() and forEach map makes a new array to collect element but forEach dose not make array is works an for loop array .filter () methods filer all element base an a condition and make a new array. 
4️⃣ What is an arrow function?
**ans: Arrow function in a es6 feature it is the updated version on traditional function it works like as simple function but it was many shorthand such as if in line function you didn't need return a single parameter din't use bracket etc.     
5️⃣ What are template literals?
**ans: Template literals is an es6 features. It gives opportunities in string we can use variable.it write backtick keyword and write multiline string . 